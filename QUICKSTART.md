# Income Expense Tracker - Quick Start Guide

## 🚀 เริ่มต้นใช้งาน PM2

### ขั้นตอนที่ 1: ติดตั้ง PM2 (ครั้งเดียว)

เปิด PowerShell **แบบ Administrator** แล้วรันคำสั่ง:

```powershell
npm install -g pm2
npm install -g pm2-windows-startup
```

---

### ขั้นตอนที่ 2: Build Frontend

```powershell
cd c:\income-expense-tracker\client
npm run build
```

> รอจนกว่า build เสร็จ (จะได้ folder `dist/`)

---

### ขั้นตอนที่ 3: เริ่มรัน PM2

```powershell
# ไปที่ root directory
cd c:\income-expense-tracker

# เริ่มรัน Backend และ Frontend
pm2 start ecosystem.config.js

# ตรวจสอบสถานะ
pm2 status
```

**ควรเห็น:**
```
┌─────┬────────────────────────────┬─────────┐
│ id  │ name                       │ status  │
├─────┼────────────────────────────┼─────────┤
│ 0   │ income-tracker-backend     │ online  │
│ 1   │ income-tracker-frontend    │ online  │
└─────┴────────────────────────────┴─────────┘
```

---

### ขั้นตอนที่ 4: ตั้งค่า Auto-Start (เปิดเครื่องรันอัตโนมัติ)

เปิด PowerShell **แบบ Administrator**:

```powershell
# ตั้งค่า startup
pm2-startup install

# บันทึก configuration
pm2 save
```

✅ **เสร็จแล้ว!** ตอนนี้ระบบจะรันอัตโนมัติทุกครั้งที่เปิดเครื่อง

---

## 🎮 คำสั่งที่ใช้บ่อย

### ดูสถานะ
```powershell
pm2 status
```

### ดู Logs
```powershell
pm2 logs
```

### Restart
```powershell
pm2 restart all
```

### Stop
```powershell
pm2 stop all
```

### Start (ถ้า stop ไว้)
```powershell
pm2 start all
```

---

## 🌐 เข้าใช้งานระบบ

เปิดเบราว์เซอร์:
- **Frontend:** http://localhost:5173
- **Backend API:** http://localhost:3001/api/health

**Login:**
- Email: `admin@example.com`
- Password: `admin123`

---

## ⚠️ แก้ไขปัญหา

### ถ้า PM2 หาไฟล์ไม่เจอ

```powershell
# ตรวจสอบว่าอยู่ใน directory ที่ถูกต้อง
cd c:\income-expense-tracker
pwd

# ตรวจสอบว่าไฟล์มีอยู่
ls ecosystem.config.js

# ถ้ามี ให้รันใหม่
pm2 start ecosystem.config.js
```

### ถ้า Frontend ไม่ทำงาน

```powershell
# Build ใหม่
cd c:\income-expense-tracker\client
npm run build

# Restart
pm2 restart income-tracker-frontend
```

### ถ้า Port ถูกใช้งานอยู่

```powershell
# หา process ที่ใช้ port
netstat -ano | findstr :3001
netstat -ano | findstr :5173

# Kill process (ใช้ PID ที่ได้)
taskkill /PID <PID> /F
```

---

## 📝 หมายเหตุ

- ต้อง **Build Frontend** ก่อนใช้ PM2 ทุกครั้ง
- ใช้ **PowerShell Administrator** สำหรับ startup setup
- ตรวจสอบ logs ด้วย `pm2 logs` ถ้ามีปัญหา
